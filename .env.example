# Environment Configuration Template
# Duplicate this file as .env or .env.local and fill in your actual values.
# NEVER commit .env to version control.

API_BASE_URL=https://api.example.com
APP_ENV=development
